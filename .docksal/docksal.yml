version: "2.1"
services:
  cli:
    environment:
      # Add $_ENV variable so both requests and drush know to load the correct settings.php file
      - DOCKSAL_LOCAL=true
      - AUTH_KEY
      - SECURE_AUTH_KEY
      - LOGGED_IN_KEY
      - NONCE_KEY
      - AUTH_SALT
      - LOGGED_IN_SALT
      - S3_UPLOADS_BUCKET
      - S3_UPLOADS_REGION
      - S3_UPLOADS_KEY
      - S3_UPLOADS_SECRET

